[build]
  command = "npm run build"
  publish = ".next"
  # Skip build when only non-app files changed (saves Netlify credits)
  # If no cached commit exists (first deploy or cache cleared), always build (exit 1)
  ignore = "if [ -z \"$CACHED_COMMIT\" ]; then exit 1; fi; git diff --quiet $CACHED_COMMIT HEAD -- src/ public/ next.config.mjs package.json package-lock.json netlify.toml brik-bds/"

[build.environment]
  NODE_VERSION = "20"
  GIT_SUBMODULES = "true"

[[plugins]]
  package = "@netlify/plugin-nextjs"

# ── Staging (branch deploys) ────────────────────────────────────────
# Any push to `staging` branch auto-deploys to:
#   staging--brik-client-portal.netlify.app
#
# To set staging-specific env vars:
#   Netlify Dashboard → Site → Environment Variables → Branch Scoping
#   Scope NEXT_PUBLIC_SITE_URL to "staging" branch with the staging URL
#
# Workflow:
#   main     → portal.brikdesigns.com  (production)
#   staging  → staging--brik-client-portal.netlify.app
#   PR #N    → deploy-preview-N--brik-client-portal.netlify.app

[context.staging]
  command = "npm run build"

[context.staging.environment]
  NEXT_PUBLIC_SITE_URL = "https://staging.portal.brikdesigns.com"

# Deploy previews for PRs
[context.deploy-preview]
  command = "npm run build"
